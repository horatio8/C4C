node_modules/
.env
.env.*
*.log
.DS_Store
.git/
.vercelignore

# server.js is the local dev server only — not deployed (api/* + static
# files cover the Vercel deployment). scripts/ and content/ stay in the
# build workspace because vercel.json's buildCommand reads them; both
# contain non-sensitive data already exposed via /content.js and the
# api/content GET handler.
server.js
